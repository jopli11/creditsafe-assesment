# Frontend

Vite + React 19 + TypeScript + Tailwind + shadcn-style UI + SWR.

## Scripts

```bash
npm install
npm run dev
npm run test
npm run lint
```

`VITE_API_BASE_URL` in `.env` should point at the API origin (same value as in Compose when not using Docker).
